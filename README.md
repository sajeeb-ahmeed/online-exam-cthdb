# Backend - MERN Online Exam

Instructions:
1. Copy `.env.example` to `.env` and set `MONGO_URI` and `JWT_SECRET`.
2. Run `npm install`.
3. Run `npm run seed` to populate sample exam.
4. Run `npm run dev` to start server.
